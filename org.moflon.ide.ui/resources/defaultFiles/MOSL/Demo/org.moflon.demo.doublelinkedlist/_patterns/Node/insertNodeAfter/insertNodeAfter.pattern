pattern insertNodeAfter 
{
	
	// Make space for new node immediately after current node
	@this : Node 
	{
		-- -next-> next
	}
	
	next : Node

	// Create outgoing links to/from new node
	@newNode : Node 
	{
		++ -previous-> this
		++ -next-> next
	}	
}