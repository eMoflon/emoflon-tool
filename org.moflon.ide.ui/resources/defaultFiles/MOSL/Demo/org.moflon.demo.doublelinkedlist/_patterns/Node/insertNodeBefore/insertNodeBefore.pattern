pattern insertNodeBefore {

	// Make space for node immediately after current node
	@this : Node {
		-- -previous-> previous
	}

	previous : Node
	
	// Create outgoing links to/from new node
	@newNode : Node {
		++ -previous-> previous
		++ -next-> this
	}	
}