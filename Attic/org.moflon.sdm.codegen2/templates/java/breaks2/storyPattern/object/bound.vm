#**##parse("$lang/default:storyPattern/object/import.vm" )
#**##if ( ! $object.noNotNullCheck )
#*   *##set( $opComment = "check object $name is really bound" )
#*   *#// $opComment
#*   *##set($depth = $utility.getPatternDepth($token))
#*   *#if ( $name == null#parse("$lang/default:storyPattern/sdmComment.vm") ) ##
#**#break pattern#if($depth>0)$depth#end; // <== not a match
#*   *#
#**##end
