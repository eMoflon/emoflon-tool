pattern removeFigure {

// Instead of just removing the reference to a Figure as done in 'checkSelected,' this pattern
// 	destroys the Figure (if it exists), removing its instance from the model entirely.

	@floor : Floor { 
		-figure-> figure
	}
	
	-- figure : Figure
}