pattern removeFigure {
	
// Instead of just removing the reference to a Figure as in 'checkSelected,' this pattern
// 	destroys the Figure completely, removing its instance from the model entirely.

	
	@floor : Floor { 
		-- -figure-> figure
	}
	
	? -- figure : Figure

}