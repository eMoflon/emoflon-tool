#**#/*
#**# * generated by Fujaba - CodeGen2
#**# */
#**#
#**##set( $file = $elem )
#**##set( $comment = $file.Comment.Text )
#**##set( $footer = $file.Footer.toString() )
#**##set( $package = $utility.nullIfEmpty($file.Package.FullPackageName) )
#**##set( $imports = $engine.getFromInformation("imports") )
#**#$!comment##
#**##if( $package )
#*   *#package $package;
#*   *#
#*   *#
#**##end
#**##foreach( $import in $imports.iteratorOfImports() )
#*   *##set( $lastDotIndex = $import.lastIndexOf(".") )
#*   *##if( $lastDotIndex > 0 )
#*      *##if( !$import.substring( 0, $lastDotIndex ).equals( $package ) )
#*         *#import $import;
#*      *##end
#*   *##end
#**##end
#**##foreach( $import in $file.iteratorOfImportedPackages() )
#*   *#import ${import.FullPackageName}.*;
#**##end
#**##foreach( $import in $file.iteratorOfImportedClasses() )
#*   *#import ${import.FullClassName};
#**##end
#**#
#**#$children
#**#$!footer