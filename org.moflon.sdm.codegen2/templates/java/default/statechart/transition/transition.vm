#**##parse("$lang/default:statechart/transition/import.vm" )
#*check for stereotype <<actioncontainer>> and generate specific code*#
#if( $elem.ParentElement.owner.hasKeyInStereotypes("ActionContainer") )
#**#### TypeOf[ ${utility} ] := de.uni_kassel.fujaba.codegen.Utility
#**#// $transition.getGuardExprText()
#**##if( $after )
#*   *#${from}.setTimeoutForAfterEvent ($timeout);
#*   *#${from}.setIdOfAfterTransition ($id);
#**##elseif( $when )
#*   *#${from}.setTimeoutForWhenEvent ($timeout);
#*   *#${from}.setIdOfWhenTransition ($id);
#**##elseif ( $to.equals("stop"))
#*   *##* do nothing *#
#**##elseif ( $transition.getGuardExprText().equals("success") )
#*   *#${utility.downFirstChar($from)}.setToSuccess($utility.downFirstChar($to));
#**##elseif ( $transition.getGuardExprText().equals("failure") )
#*   *#${utility.downFirstChar($from)}.setToFailure($utility.downFirstChar($to));
#**##else
#*   *##* do output for debugging *#
#*   *#//${utility.downFirstChar($from)}.addToFollowers("$transition.getName()", $utility.downFirstChar($to));
#**##end

#else
#**##if( $after )
#*   *#${from}.setTimeoutForAfterEvent ($timeout);
#*   *#${from}.setIdOfAfterTransition ($id);
#**##end
#**##if( $when )
#*   *#${from}.setTimeoutForWhenEvent ($timeout);
#*   *#${from}.setIdOfWhenTransition ($id);
#**##end
#**#myFReactive.makeTransition ($from, $to, "$eventName", $utility.quote($guardName), $utility.quote($actionName));
#end